services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: anket
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - anket-postgres:/var/lib/postgresql/data

volumes:
  anket-postgres:
